import 'package:flutter/material.dart';

class HalloWordScreen extends StatelessWidget{
  const HalloWordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child:
          Text("Olá Mundo")
    )
    );
  }
}
